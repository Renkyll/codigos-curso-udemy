n = int(input('Quantos números serão digitados: '))
lis = (0 for x in range(n))
s = 0

for i in range(0, n):
    n2 = float(input('Digite um número: '))
    s = s + n2

print(f'O resultado da soma é: {s:.2f}')
